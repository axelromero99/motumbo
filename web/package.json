{
  "name": "motumbo-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "tsc --noEmit && node ../scripts/test-sim.mjs && node ../scripts/test-lockstep.mjs && node ../scripts/audit-levels.mjs && node ../scripts/audit-reach.mjs && node ../scripts/test-modes.mjs",
    "smoke": "node ../scripts/smoke-render.mjs"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "playwright": "^1.61.1",
    "typescript": "^6.0.3",
    "vite": "^8.1.3"
  }
}
